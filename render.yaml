services:
  - type: web
    name: mittare-sattahip-demo
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APP_INSTANCE_DIR
        value: /var/data
      - key: ADMIN_PASSWORD
        sync: false
      - key: LINE_CHANNEL_ACCESS_TOKEN
        sync: false
      - key: LINE_CHANNEL_SECRET
        sync: false
      - key: ADMIN_LINE_USER_ID
        value: Ueaebf5b870fcdd317383855ff445e460
      - key: SECRET_KEY
        generateValue: true
    disk:
      name: mittare-data
      mountPath: /var/data
      sizeGB: 1
